# Figma — used by: npm run sync:tokens
# Create a token: Figma → Settings → Security → Personal access tokens
# Scope: file_variables:read (Variables API; plan must allow GET variables/local)
FIGMA_ACCESS_TOKEN=

# Optional overrides
FIGMA_FILE_KEY=WRrQVjZBnyWDdtFfgwdHqp
FIGMA_COLLECTION_NAME=New DS / Primitive / Color
